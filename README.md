# Free-AI-Text-to-Speech-Generator
This repository contains a **Python script that converts large text files into spoken audio using Google's Gemini AI Text-to-Speech API**.  The script reads a text file, splits it into smaller chunks, sends each chunk to **Google Gemini TTS**, and saves the generated speech as **.wav audio files**.  
